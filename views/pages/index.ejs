<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <h1>Dota 2</h1>
    <% Object.keys(heros).forEach(function(id){ %>
      <div>
        <a href="<%= heros[id].link %>"><%= heros[id].title %></a>
      </div>
    <% }) %>
  </body>
</html>
